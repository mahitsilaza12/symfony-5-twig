
{{ include('page/layouts/header.html.twig') }}
{% block body %}
    <div class="card">
        <div class="card-body">
            <h5 class="card-title">Liste des Actualites</h5>
            <button>
                <a class="btn btn-default btn-outline sbold" href="{{ path('app_create_actualite')  }}" >
                    <i class="fa fa-plus"></i> Ajouter
                </a>
            </button>
            <div class="table-responsive">
                <table id="zero_config" class="table table-striped table-bordered">
                    <thead>
                    <tr>
                        {# <th>Id</th> #}
                        <th>Nom du fichier</th>
                        <th>Description</th>
                        <th span="2">Action</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for actualites in actualite %}
                    <tr>
                        {# <td>{{ actualites.id }}</td> #}
                        <td>{{ actualites.fichier }}</td>
                        <td>{{ actualites.description }}</td>
                        <td>
                            <a class="btn btn-success btn-outline sbold" href="{{path('app_show_actualit',{id : actualites.id} )}}">
                                <i class="fa fa-edit"></i> Modifier</a>
                            <a class="btn btn-danger btn-outline sbold"  href="{{path('app_delete_actualit',{id:actualites.id})}}">
                                <i class="fa fa-delete"></i> Supprimer</a>
                        </td>
                        {% else %}
                            <p>pas d'actualite</p>
                        {% endfor %}
                    </tr>
                    </tbody>
                    <tfoot>
                    <tr>
                        <th>Nom du fichier</th>
                        <th>Description</th>
                        <th span="2">Action</th>
                    </tr>
                    </tfoot>
                </table>
            </div>

        </div>
    </div>
{% endblock %}
</main>
{{ include('page/layouts/footer.html.twig') }}
